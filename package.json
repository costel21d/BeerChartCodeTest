{
  "name": "beers-chart-by-range",
  "version": "1.0.0",
  "description": "Count all the beers and create ranges from 1 to 2, 3 to 4, and so on.",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "react": "17.0.*",
    "react-dom": "17.0.*",
    "react-native-web": "0.18.*",
    "react-scripts": "3.4.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.21"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
